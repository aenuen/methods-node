import { uniCode_decode } from '../src/uniCode/decode'
import { uniCode_encode } from '../src/uniCode/encode'

export function _uniCode_decode(string) {
  return uniCode_decode(string)
}

export function _uniCode_encode(string) {
  return uniCode_encode(string)
}