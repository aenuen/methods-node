import { urlString_oneQuery } from '../src/urlString/oneQuery'
import { urlString_queryJson } from '../src/urlString/queryJson'

export function _urlString_oneQuery(urlString, name) {
  return urlString_oneQuery(urlString, name)
}

export function _urlString_queryJson(urlString) {
  return urlString_queryJson(urlString)
}