import { format_date } from '../src/format/date'
import { format_datetime } from '../src/format/datetime'
import { format_domain } from '../src/format/domain'
import { format_email } from '../src/format/email'
import { format_external } from '../src/format/external'
import { format_idCard } from '../src/format/idCard'
import { format_imageBase } from '../src/format/imageBase'
import { format_ip } from '../src/format/ip'
import { format_mobile } from '../src/format/mobile'
import { format_number } from '../src/format/number'
import { format_price } from '../src/format/price'
import { format_telephone } from '../src/format/telephone'
import { format_url } from '../src/format/url'
import { format_username } from '../src/format/username'
import { format_zip } from '../src/format/zip'

export function _format_date(value) {
  return format_date(value)
}

export function _format_datetime(value) {
  return format_datetime(value)
}

export function _format_domain(value) {
  return format_domain(value)
}

export function _format_email(value) {
  return format_email(value)
}

export function _format_external(value) {
  return format_external(value)
}

export function _format_idCard(value) {
  return format_idCard(value)
}

export function _format_imageBase(value) {
  return format_imageBase(value)
}

export function _format_ip(value) {
  return format_ip(value)
}

export function _format_mobile(value) {
  return format_mobile(value)
}

export function _format_number(value) {
  return format_number(value)
}

export function _format_price(value) {
  return format_price(value)
}

export function _format_telephone(value) {
  return format_telephone(value)
}

export function _format_url(value) {
  return format_url(value)
}

export function _format_username(value) {
  return format_username(value)
}

export function _format_zip(value) {
  return format_zip(value)
}