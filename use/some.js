import { some_februaryDays } from '../src/some/februaryDays'
import { some_maxZIndex } from '../src/some/maxZIndex'
import { some_pluralize } from '../src/some/pluralize'
import { some_randomColor } from '../src/some/randomColor'
import { some_weekCn } from '../src/some/weekCn'
import { some_weekEn } from '../src/some/weekEn'
import { some_whetherLeapYear } from '../src/some/whetherLeapYear'
import { some_yearMonthDays } from '../src/some/yearMonthDays'

export function _some_februaryDays(year) {
  return some_februaryDays(year)
}

export function _some_maxZIndex() {
  return some_maxZIndex()
}

export function _some_pluralize(value, label) {
  return some_pluralize(value, label)
}

export function _some_randomColor() {
  return some_randomColor()
}

export function _some_weekCn(week) {
  return some_weekCn(week)
}

export function _some_weekEn(week) {
  return some_weekEn(week)
}

export function _some_whetherLeapYear(year) {
  return some_whetherLeapYear(year)
}

export function _some_yearMonthDays(year, month) {
  return some_yearMonthDays(year, month)
}