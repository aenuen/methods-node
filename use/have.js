import { have_assign } from '../src/have/assign'
import { have_cn } from '../src/have/cn'

export function _have_assign(value, assign) {
  return have_assign(value, assign)
}

export function _have_cn(value) {
  return have_cn(value)
}